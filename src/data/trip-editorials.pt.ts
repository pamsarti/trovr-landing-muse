/**
 * Portuguese editorial overlay (Phase 2 — content translation).
 *
 * Keyed by the SAME trip id as TRIP_EDITORIALS in trip-editorials.ts. Fill each
 * entry with the hand-written Portuguese editorial when ready; any id left out
 * falls back to the English editorial automatically (see editorialFor(id,
 * locale)). Partial translation is fine — it degrades to English per trip.
 */
export const TRIP_EDITORIALS_PT: Record<string, string> = {
  // "egypt-kite-dragonfly": "…tradução em português…",
};
